# 추가 의존성 모듈들
# 패킷 폴더 모니터링을 위한 watchdog
watchdog>=3.0.0

# 비동기 파일 작업을 위한 aiofiles
aiofiles>=23.0.0

# WebSocket 실시간 통신
python-multipart>=0.0.6

# 템플릿 엔진 (HTML 페이지용)
jinja2>=3.1.0

# 정적 파일 서빙
python-multipart

# 기존 requirements.txt에 추가해야 할 항목들
# (이미 있는지 확인 후 추가)

# 데이터 과학 및 분석
numpy>=1.24.0
pandas>=2.0.0

# 딥러닝 (선택적)
# tensorflow>=2.13.0  # 큰 용량이므로 필요시에만
scikit-learn>=1.3.0

# 웹 프레임워크
fastapi>=0.100.0
uvicorn[standard]>=0.23.0

# 데이터베이스
sqlite3  # Python 내장 모듈

# 비동기 처리
asyncio  # Python 내장 모듈
aiohttp>=3.8.0

# 로깅 및 유틸리티
python-dateutil>=2.8.0
pathlib  # Python 내장 모듈

# JSON 처리 (Python 내장)
json  # Python 내장 모듈

# 정규 표현식 (Python 내장)
re  # Python 내장 모듈

# 수학 연산
math  # Python 내장 모듈
random  # Python 내장 모듈

# 시간 처리
datetime  # Python 내장 모듈